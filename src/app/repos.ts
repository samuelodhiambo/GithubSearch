import { Url } from "url";

export class Repos {
    constructor(id: number, name: string, owner: any[], html_url: Url, created_at: Date, topics: any[], stargazers_count: number, forks_count: number) {
        
    }
}
